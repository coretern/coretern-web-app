export const metadata = {
    title: 'Verify Certificate | CoreTern',
    description: 'Verify the authenticity of a CoreTern certificate by entering the unique Certificate ID.',
};

export default function VerifyLayout({ children }: { children: React.ReactNode }) {
    return <>{children}</>;
}
