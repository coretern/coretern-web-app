export const metadata = {
    title: 'Dashboard | CoreTern',
    description: 'Manage your internship enrollments, certificates, and support tickets from your CoreTern dashboard.',
};

export default function DashboardLayout({ children }: { children: React.ReactNode }) {
    return <>{children}</>;
}
